{
  "name": "dual-ball-mirror",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":   "esbuild src/main.ts --bundle --platform=browser --outfile=game.js --watch --sourcemap",
    "build": "esbuild src/main.ts --bundle --platform=browser --target=es2017 --minify --outfile=game.js",
    "test":  "vitest run"
  },
  "dependencies": {
    "matter-js": "^0.19.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "esbuild": "^0.21.0",
    "vitest": "^1.6.0",
    "@types/matter-js": "^0.19.0"
  }
}
